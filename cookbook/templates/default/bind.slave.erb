// Managed by Chef, Bork Off.
// Bind9 Test Master Configuration

options {
	directory "/var/lib/bind";
	masterfile-format text;
	dnssec-validation auto;
	auth-nxdomain no;    # conform to RFC1035
	listen-on-v6 { none; };

	request-ixfr yes;

	allow-transfer { any; };
	allow-recursion { none; };
	notify false;
};

zone "example.com" {
	type slave;
	file "/var/cache/bind/example.com.db";
	masters { 192.168.50.5; };
};
